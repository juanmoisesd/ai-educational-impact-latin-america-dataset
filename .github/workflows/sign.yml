name: Sign
on:
  release:
    types: [published]
jobs:
  sign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sign Artifacts
        run: echo "Signing artifacts..."
